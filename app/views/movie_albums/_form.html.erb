<%= form_with(model: movie_album, local: true) do |form| %>
  <% if movie_album.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(movie_album.errors.count, "error") %> prohibited this movie_album from being saved:</h2>

      <ul>
        <% movie_album.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
